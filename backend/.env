# Supabase Database URL (PostgreSQL)
# Format: postgresql://[user]:[password]@[host]:[port]/[db]
# For MVP, we default to SQLite. Replace with Supabase Postgres URL for production.
DATABASE_URL=sqlite:///./cleansight.db

# Cloudinary or Supabase Storage (Mocked for now)
STORAGE_API_KEY=mock-key
